# PromptAgent Audit Report
*Date*: 2026-05-07
*Author*: Claude(under Simone's directive)
*Scope*: Phase 0 of v1.0 完善计划

---

## TL;DR

prompt-agent **不是从零开始**——它已经是一个 0.6.0 版本、跑得通的项目:
- ✅ FastAPI 后端 47 个 endpoint,功能完整
- ✅ Tauri 桌面壳 + 一份原生 JS+HTML 工作台已实现 3 个工作区
- ✅ Knowledge OS 数据 schema 完整,只是几乎空白
- ✅ 4 种 LLM provider 适配 + presets 管理完整
- ✅ Personal Wiki(7 份个人资料)结构已建好,内容空
- ✅ Skills 机制有最小骨架(`level_up_capture.md`)
- ⚠️ **桌面端是原生 JS 而不是 React**,这是 v1.0 必须重写的部分
- ⚠️ src-tauri 是最小骨架(11 行 main.rs),没有任何 Tauri 命令、shell extension 注册
- ❌ 无学习计划/掌握度追踪相关代码与文件
- ❌ 无 study 体系连接(完全独立)
- ❌ 无 PDF/图片解析能力
- ❌ 浏览器扩展未在仓库内出现(状态未知)

**结论**:v1.0 的 90% 是**前端重写 + 数据迁移 + 学习计划新模块 + Tauri Rust 命令**,后端只需 30% 微补。

---

## 1. 后端(FastAPI)

### 已有 47 个 endpoint 全清单

#### Health & Settings(5)
| 路由 | 方法 | 状态 | 备注 |
|------|------|------|------|
| `/health` | GET | ✅ | OK |
| `/api/settings/model` | GET/POST | ✅ | 4 provider 切换,API key 不外泄 |
| `/api/settings/provider-presets` | GET | ✅ | mock/openai/ollama/deepseek 等预设 |

#### 提示词功能(3)
| 路由 | 方法 | 状态 | Phase 影响 |
|------|------|------|-----------|
| `/api/generate` | POST | ✅ 完整 | Phase 6 增强:加自动模式 |
| `/api/prompt-lab/test` | POST | ✅ | OK |
| `/api/prompt-lab/compare` | POST | ✅ | OK |

#### Capture(浏览器→桌面端通道)(2)
| 路由 | 方法 | 状态 | 备注 |
|------|------|------|------|
| `/api/capture` | POST | ✅ | 接受 prompt/level_up 两种 action |
| `/api/capture/latest` | GET | ✅ | 桌面端轮询获取 |

#### Level Up(1)
| `/api/level-up` | POST | ✅ | 写入 sources + claims + review_queue |

#### Knowledge OS - Sources(5 + folder)
| `/api/knowledge-os/sources` | GET 列表 | ✅ |
| `/api/knowledge-os/sources/{id}` | GET/PUT/DELETE | ✅ |
| `/api/knowledge-os/sources/{id}/open` | POST | ✅ 调系统打开文件 |
| `/api/knowledge-os/search` | POST | ✅ |
| `/api/knowledge-os/open-folder` | POST | ✅ |

#### Knowledge OS - Claims(3)
| `/api/knowledge-os/claims` | GET 列表 | ✅ |
| `/api/knowledge-os/claims/{id}` | PUT/DELETE | ✅ |

#### Level Up Review Queue(5)
| `/api/level-up/review-queue` | GET | ✅ |
| `/api/level-up/review/{id}` | GET/PUT | ✅ |
| `/api/level-up/review/{id}/approve` | POST | ✅ 批准入主图谱 |
| `/api/level-up/review/{id}/reject` | POST | ✅ |

#### Knowledge OS - Graph(8)
| `/api/knowledge-os/graph/nodes` | GET 列表 | ✅ |
| `/api/knowledge-os/graph/nodes/{id}` | PUT/DELETE | ✅ |
| `/api/knowledge-os/graph/edges` | GET 列表 | ✅ |
| `/api/knowledge-os/graph/edges/{id}` | PUT/DELETE | ✅ |
| `/api/knowledge-os/graph/open-folder` | POST | ✅ |

#### Knowledge OS - Personal Wiki(4)
| `/api/knowledge-os/personal/files` | GET 列表 | ✅ |
| `/api/knowledge-os/personal/files/{id}` | GET/PUT | ✅ |
| `/api/knowledge-os/personal/open-folder` | POST | ✅ |
| `/api/knowledge-os/personal/summary-test` | POST | ✅ |

#### Knowledge OS - Logs(2)
| `/api/knowledge-os/logs` | GET | ✅ |
| `/api/knowledge-os/logs/open` | POST | ✅ |

#### Skills & Extension(2)
| `/api/skills/level-up` | GET | ✅ 元数据 |
| `/api/extension/status` | GET | ✅ |

### 后端缺什么(Phase 接管)

| 缺失能力 | 紧急度 | 接管阶段 |
|---------|--------|----------|
| `/api/inbox/import`(支持 PDF/图片/批量上传) | 高 | **Phase 3** |
| `/api/plans/*`(学习计划 CRUD + 复习) | 高 | **Phase 5** |
| `/api/wiki/pages`(通用 .md 浏览,不只 sources/personal) | 中 | **Phase 4** |
| `/api/wiki/graph`(全 vault 的 nodes+edges 聚合,不只 KO) | 中 | **Phase 4** |
| `/api/inbox/*`(列出待处理、批量分析、生成 wiki) | 高 | **Phase 7** |
| `/api/prompt/auto-classify`(自动模式判断要不要读知识库) | 中 | **Phase 6** |
| 服务器推送(SSE)用于批处理进度 | 中 | Phase 7 |

### 路径配置(关键!)

```python
# app/core/config.py 第 17 行
knowledge_os_path: Path = PROJECT_ROOT / "knowledge_os"
```

这是**整个迁移的支点**——只要把 study 数据迁到 `prompt-agent/knowledge_os/`,后端不需要改代码,所有 47 个 endpoint 立即接管新数据。

---

## 2. Tauri 桌面端

### 现状(完全可用但极简)

| 文件 | 现状 | 说明 |
|------|------|------|
| `src-tauri/Cargo.toml` | tauri 2.x,无任何 plugins | 缺 plugin-fs / plugin-shell / plugin-os |
| `src-tauri/src/main.rs` | 11 行,只 `Builder::default().run()` | **零自定义 command** |
| `src-tauri/tauri.conf.json` | (未读)默认配置 | |
| `src/index.html` | 完整布局,3 个工作区按钮 | 原生 HTML |
| `src/app.js` | 调 8787 API 的状态机 | 原生 JS,无框架 |
| `src/styles.css` | (未读) | 原生 CSS |
| `dist/*` | 已 build 过的产物 | |
| `node_modules/@tauri-apps/cli@2.x` | 已装 | OK |

### 桌面端缺什么

| 缺失 | 接管阶段 |
|------|----------|
| **重写为 React + Vite + TS + shadcn/ui** | **Phase 2** |
| Tauri commands(读本地文件、写 inbox、注册 shell menu) | Phase 3 + 6 |
| Windows Shell Extension(右键 .md/.pdf/.txt) | **Phase 6** |
| Tauri plugin-fs / plugin-shell / plugin-dialog 装上 | Phase 2 |
| 启动时自动拉起后端(spawn `python -m uvicorn`) | Phase 2 |

### 风险

- 重写到 React 会影响**已 build 的 dist/**——需要保留 `src.legacy/` 作为回退
- Tauri 2 + Windows Shell Extension 在中文路径(`prompt Agent` 带空格)下有报告过 path 转义问题——Phase 6 必须先验证

---

## 3. Knowledge OS 现状

### 文件结构

```
knowledge_os/
├── index.md                  ← 4 行说明
├── log.md                    ← 1 条 init 记录
├── claims/claims.jsonl       ← 1 条记录(描述桌面端自身)
├── graph/nodes.jsonl         ← 3 条记录(prompt_lab/knowledge_os/settings)
├── graph/edges.jsonl         ← 1 条边
├── graph/review_queue.jsonl  ← 1 条 pending(自描述)
├── skills/level_up_capture.md ← 4 行框架
└── wiki/
    ├── personal/             ← 7 个空白模板文件
    │   ├── profile.md
    │   ├── preferences.md
    │   ├── goals.md
    │   ├── current_projects.md
    │   ├── writing_style.md
    │   ├── learning_style.md
    │   └── decision_history.md
    └── sources/
        └── desktop-workspaces.md  ← 自描述 source
```

### 评估

- 几乎空白,**无价值数据担心被覆盖** —— 适合整体接管
- Schema 设计良好且与后端代码一致,**Phase 1 迁移会保留这套 schema**
- llm-wiki 数据模型(Markdown + frontmatter)与 Knowledge OS Markdown 部分**100% 兼容**
- llm-wiki 的 `[[wikilink]]` 可以直接放进 `wiki/` 任何子目录
- finance/english/coding 路线图作为新的 `plans/*` 节点

---

## 4. study/ 现状(迁移源)

按用户指令"迁 llm-wiki + finance/english/coding 全部进走":

### 要迁的内容

| 来源 | 目标 | 类型 |
|------|------|------|
| `study/llm-wiki/wiki/` 全部 31 个页面 | `prompt-agent/knowledge_os/wiki/` | **覆盖**(已确认无价值数据) |
| `study/llm-wiki/raw/` 全部 | `prompt-agent/knowledge_os/raw/` | **新建** |
| `study/llm-wiki/.obsidian/` | `prompt-agent/knowledge_os/.obsidian/` | **新建**(graph 配置等) |
| `study/llm-wiki/CLAUDE.md` | `prompt-agent/knowledge_os/CLAUDE.md` | **新建** |
| `study/llm-wiki/README.md` | `prompt-agent/knowledge_os/README.md` | **覆盖**(改写指引到新位置) |
| `study/llm-wiki/inbox-uploader.html` | `prompt-agent/legacy/inbox-uploader.html` | **归档**(被 Phase 3 桌面端取代) |
| `study/llm-wiki/OPEN-IN-OBSIDIAN.md` | `prompt-agent/RELOCATE-OBSIDIAN.md`(改写) | **改写** |
| `study/raw/finance/` | `prompt-agent/knowledge_os/raw/finance/` | **新建** |
| `study/raw/english/` | `prompt-agent/knowledge_os/raw/english/` | **新建** |
| `study/raw/coding/` | `prompt-agent/knowledge_os/raw/coding/` | **新建** |
| `study/wiki/finance-路线图.md` | `prompt-agent/knowledge_os/wiki/plans/finance-roadmap.md` | **改名** |
| `study/wiki/english-路线图.md` | `prompt-agent/knowledge_os/wiki/plans/english-roadmap.md` | **改名** |
| `study/wiki/coding-路线图.md` | `prompt-agent/knowledge_os/wiki/plans/coding-roadmap.md` | **改名** |
| `study/wiki/00-学习进度.md` | `prompt-agent/knowledge_os/wiki/plans/learning-progress.md` | **改名** |
| `study/wiki/TEMPLATE-*.md` | `prompt-agent/knowledge_os/wiki/_templates/legacy/` | **保留**(老模板与新模板共存) |
| `study/schema/AI-RULES.md` | `prompt-agent/knowledge_os/_schema/AI-RULES.md` | **新建** |
| `study/schema/WORKFLOW.md` | `prompt-agent/knowledge_os/_schema/WORKFLOW.md` | **新建** |
| `study/schema/TEMPLATE-验收标准.md` | `prompt-agent/knowledge_os/_schema/TEMPLATE-验收标准.md` | **新建** |

### 双写期方案

迁完后,**study 目录改名为 `study.archived-2026-05-07/`**(只读),**不立即删除**。
等 prompt-agent 跑通 1-2 周后再正式删除。这一点会写进 RELOCATE-OBSIDIAN.md 提醒。

---

## 5. 风险与已知坑

### 高风险

1. **Obsidian vault 必须重新 "Open folder as vault"**——Obsidian 不会自动跟随。已写一份重定位指南。
2. **Tauri build 缓存(target/)很大**——迁移不动它,只迁源代码与数据。
3. **路径中的中文/空格**:`prompt Agent` 带空格,Phase 6 Shell Extension 注册时必须用引号包路径。

### 中风险

4. **knowledge_os/wiki/ 已存在 desktop-workspaces.md 且与即将迁入的 wiki 命名冲突**?— 已检查无冲突,sources 命名 `desktop-workspaces.md` vs llm-wiki 的 sources 都带日期前缀。
5. **personal/ 7 个文件如何与 llm-wiki 合并**?— 不合并。llm-wiki 没有 personal 概念,直接保留 prompt-agent 现有 personal/ 文件。

### 低风险

6. **CLAUDE.md 路径声明 §0.1 引用 study/**——迁完要改 §0.1 改成新位置。

---

## 6. 给 Phase 1 的明确动作清单

按时间顺序:

1. ✅ 在 prompt-agent 仓库下建 `legacy/`、`docs/`(本文)。
2. 备份现有 knowledge_os/ 为 `knowledge_os.before-migration-2026-05-07/`。
3. 拷贝 study/llm-wiki/ 内容到 prompt-agent/knowledge_os/(按上面映射表)。
4. 拷贝 study/raw/finance|english|coding 到 prompt-agent/knowledge_os/raw/。
5. 拷贝 study/wiki/路线图 → knowledge_os/wiki/plans/(并改文件名)。
6. 拷贝 study/schema/ → knowledge_os/_schema/。
7. 改写 knowledge_os/README.md 与 CLAUDE.md(让它们意识到自己已不在 study 下)。
8. 在 .obsidian/graph.json 中把 inbox 和 _templates 的过滤规则保留。
9. 写 RELOCATE-OBSIDIAN.md。
10. 把 study/ 改名为 study.archived-2026-05-07/。
11. 在 prompt-agent/knowledge_os/log.md + study.archived/llm-wiki/wiki/log.md 都追加迁移记录。

---

## 7. Phase 1 不做什么

- 不动 prompt-agent 已有任何 .py、.rs、.js 代码
- 不动 personal/ 7 个文件(等用户决定要不要填)
- 不删 study,只改名归档
- 不改 CLAUDE.md 内的"主题/目标"部分,只改路径声明
- 不接 React、不写新 endpoint(Phase 2/3 才动)
